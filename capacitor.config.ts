import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'lo.que.hizo.tache.en.su.tiempo',
  appName: 'lo-que-hizo-tache-en-su-tiempo',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
